# gateway-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wpai-file
  labels:
    app: wpai-file
  namespace: wpai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wpai-file
  template:
    metadata:
      labels:
        app: wpai-file
    spec:
      containers:
        - name: wpai-file
          image: wpai-file:v1.0.0
          ports:
            - containerPort: 9300
          resources:
            requests:
              memory: '256Mi'
              cpu: '100m'
            limits:
              memory: '512Mi'
              cpu: '500m'
          args:
            [
              'nohup',
              'java',
              '-jar',
              '/usr/local/wpai/ruoyi-modules-file.jar',
              '>',
              '/usr/local/wpai/logs/ruoyi-modules-file/nohup.log',
              '&',
            ]
